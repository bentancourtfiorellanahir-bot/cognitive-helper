# Icons

These SVG icons are lightweight local assets used by Cognitive Helper.

- `brain.svg` → cognitive support / planning
- `focus.svg` → focus mode / attention
- `spark.svg` → small-step momentum / clarity

They are intentionally simple so they can be styled easily with CSS if needed.